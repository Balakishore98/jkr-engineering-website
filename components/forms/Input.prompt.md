Labeled text field — mono uppercase label, low-radius steel border, amber focus ring.

```jsx
<Input label="Full name" placeholder="Your name" />
<Input label="Phone" type="tel" hint="We reply within one business day." />
<Input label="Email" error hint="Enter a valid email." />
```

- `label`, `hint`, `error`, plus all native input attributes (`type`, `placeholder`, …)
